node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vite/
coverage/
.cache/

# Playwright — the HTML report and the per-run artefacts. Browsers live in the
# user cache (`npx playwright install`), not here.
playwright-report/
test-results/
blob-report/
firebase-debug.log
firestore-debug.log
database-debug.log

# Service-account key for `npm run seed-vault`. Never commit one.
.secrets/

# Tool wiring — symlinks onto shared context and AGENTS.md. Never commit.
CLAUDE.md
.cursor/rules/00-core.mdc
.cursor/rules/typescript.mdc
.cursor/rules/testing.mdc
.cursor/rules/docs.mdc
.cursor/rules/session-close.mdc
